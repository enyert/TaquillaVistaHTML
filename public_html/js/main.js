/* la clase Taquilla contiene todos los datos importantes de la taquilla de
 * apuestas lista para el "prototipaje" de la misma
 * 
 * @param {type} id
 * @param {type} centroApuestaId
 * @param {type} nombre
 * @param {type} username
 * @param {type} password
 * @param {type} created
 * @param {type} modified
 * @param {type} status
 * @param {type} serial
 * @param {type} anular
 * @param {type} statusReg
 * @param {type} admin
 * @returns {Taquilla}
 * 
 **/

function Taquilla(id, centroApuestaId, nombre, username, password, created, modified, status, serial, anular,
                    statusReg, admin){
                        this.id = id;
                        this.centroApuestaId = centroApuestaId;
                        this.nombre = nombre;
                        this.username = username;
                        this.password = password;
                        this.created  = created;
                        this.modified = modified;
                        this.status = status;
                        this.serial = serial;
                        this.anular = anular;
                        this.statusReg = statusReg;
                        this.admin = admin;
                    }
                    
/**
 * @argument {int} idBoleto almacena el id del boleto que sera vendido
 * */
Object.prototype.venderBoleto = function(idBoleto){
    if(id.isPrototypeOf(Object)){
        
    }
}                 
